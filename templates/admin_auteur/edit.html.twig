{% extends 'base.html.twig' %}

{% block title %}Modifier l'auteur - Admin La librairie{% endblock %}

{% block body %}
    <div class="my-3">
        <a href="{{ path('app_admin_auteur_index') }}" class="btn btn-dark my-2">Retour à l'index</a>
        <h1>Modifier l'auteur {{ auteur.pseudo ? auteur.pseudo : auteur.prenom ~ ' ' ~ auteur.nom }}</h1>
    </div>

    {{ include('admin_auteur/_form.html.twig', {'button_label': ('<i class="fa fa-floppy-o" aria-hidden="true"></i>')|raw}) }}

    {{ include('admin_auteur/_delete_form.html.twig') }}
{% endblock %}
