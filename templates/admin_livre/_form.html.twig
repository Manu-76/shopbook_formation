{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mt-3">{{ button_label|default('<i class="fa fa-floppy-o" aria-hidden="true"></i>')|raw }}</button>
{{ form_end(form) }}
